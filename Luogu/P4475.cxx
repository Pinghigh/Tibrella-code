#include <algorithm>
#include <iostream>

using std::cin;
using std::cout;
using std::max;
using std::min;

using i32 = int;

struct Node {
    i32 lc, rc;
};

int main() {
    std::ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    return 0;
}