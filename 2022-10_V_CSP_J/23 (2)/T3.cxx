#include <iostream>

using namespace std;

int main() {
    int n,m,a,T,b;
    cin >> T;
    cin >> n >> m;
    if (n == m && n == 1) {
        cin >> a;

        cout << ((a%2)==1) ;
    } else {
        cout << 114514;
    }

    return 0;
}